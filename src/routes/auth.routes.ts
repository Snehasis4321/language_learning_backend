// COMMENTED OUT - Firebase Auth Routes (keeping for reference)
// Migrated to Appwrite Auth - see src/routes/appwrite-auth.routes.ts

import { Router } from 'express';

const router: Router = Router();

// Legacy Firebase auth routes - no longer used
// All auth functionality has been moved to appwrite-auth.routes.ts

export default router;
